<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE generatorConfiguration
		PUBLIC "-//mybatis.org//DTD MyBatis Generator Configuration 1.0//EN"
		"http://mybatis.org/dtd/mybatis-generator-config_1_0.dtd">
<generatorConfiguration>
	<!--数据加载的环境-->
	<properties resource="application-test.yml"/>

	<context id="Mysql" targetRuntime="MyBatis3Simple" defaultModelType="flat">
		<!--处理sql中的`符号-->
		<property name="beginningDelimiter" value="`"/>
		<property name="endingDelimiter" value="`"/>

		<!--添加自定义的继承接口-->
		<plugin type="tk.mybatis.mapper.generator.MapperPlugin">
			<property name="mappers" value="tk.mybatis.mapper.common.Mapper"/>
			<property name="lombok" value="Data"/>
		</plugin>

		<!--数据源配置-->
		<jdbcConnection driverClass="${driver-class-name}"
						connectionURL="${url}"
						userId="${username}"
						password="${password}">
			<property name="nullCatalogMeansCurrent" value="true"/>
		</jdbcConnection>

		<!--model包路径-->
		<javaModelGenerator targetPackage="com.example.cp.entity" targetProject="src/main/java"/>
		<!--mapper.xml包路径-->
		<sqlMapGenerator targetPackage="mybatis.mapper" targetProject="src/main/resources"/>
		<!--mapper包路径-->
		<javaClientGenerator targetPackage="com.example.cp.mapper" targetProject="src/main/java" type="XMLMAPPER"/>
		<!--表配置，tableName支持%，表示全部生成； 生成多个表可以配置多个table-->
		<table tableName="sys_role" domainObjectName="SysRole">
			<!--mysql 配置-->
			<generatedKey column="id" sqlStatement="Mysql" identity="true"/>
		</table>

		<table tableName="user_info" domainObjectName="UserInfo">
			<!--mysql 配置-->
			<generatedKey column="id" sqlStatement="Mysql" identity="true"/>
		</table>

		<table tableName="user_password" domainObjectName="UserPassword">
			<!--mysql 配置-->
			<generatedKey column="id" sqlStatement="Mysql" identity="true"/>
		</table>
	</context>
</generatorConfiguration>